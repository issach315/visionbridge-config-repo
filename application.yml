# Global configuration for all services
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  
logging:
  level:
    com.visionbridge: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
